/* window.addEventListener('load', () => {
    const socket = new io();

    window.addEventListener('click', () => {
        console.log('clic sur la page');
        socket.emit('message', 'clic sur la page');
    })
}); */